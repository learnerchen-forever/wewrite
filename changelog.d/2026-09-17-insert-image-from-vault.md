---
type: feat
scope: editor
---

Inserting an image into a note has its own command and menu entry: **Insert Image from Vault** opens the same thumbnail grid the cover picker uses — with a folder dropdown and a filename filter — instead of Obsidian's list of bare file names, which stops being readable once a vault holds more than a few dozen photos.
